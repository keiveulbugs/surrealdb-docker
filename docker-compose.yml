version: '3.8'

services:
  surrealdb:
    image: surrealdb/surrealdb:latest
    container_name: surrealdb
    ports:
      - "8000:8000"
    volumes:
      - ./surrealdb_data:/data
    command: 
      - start
      - --log=info
      - --user=root
      - --pass=root
      - file:/data/database.db
    networks:
      - app-network
    restart: unless-stopped

  rust-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rust-app
    depends_on:
      - surrealdb    
    # At the moment these don't do anything, can be used tho if wished   
    environment:
      - DATABASE_URL=ws://127.0.0.1:8000/
      - DATABASE_USER=root
      - DATABASE_PASS=root
      - DATABASE_NS=testns
      - DATABASE_DB=testdb
    networks:
      - app-network
    restart: unless-stopped

networks:
  app-network:
    driver: bridge

volumes:
  surrealdb_data: